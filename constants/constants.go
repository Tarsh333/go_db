package constants

var Constants = struct {
	InvalidParams     string
	InvalidDataFormat string
}{
	InvalidParams:     "Invalid request Params",
	InvalidDataFormat: "Invalid data format. not a valid json",
}
